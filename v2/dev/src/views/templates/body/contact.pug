section.section.contact
  h2.section__title#contact Contacto
  form.contact__form.form
    .form__data
      .form__inputs
        input(type="text", placeholder="Nombre", name="name").form__input
        input(type="email", placeholder="Correo", name="email").form__input
      .form__message
        textarea(name="message", cols="30", rows="10", placeholder="Mensaje")
      footer.form__footer
        a(href="#").btn.form__btn envíar 
  .btn-container:  a(href="#").btn.btn-block Leer más